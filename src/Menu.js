import * as React from 'react';

const Menu = () => {
  return (
    <div>
      <button>Back</button>
      <button>Prev</button>
      <button>Next</button>
    </div>
  );
};

export default Menu;
